#pragma once

#define TIME_LIMIT 2.5
#define C 0.7
#define SEARCH_LIMIT 2000000
#define MAX_SIZE 12
#define OUTPUT 0
typedef unsigned uint;

#include <cstring>
#define CLEAR(arr, v) memset(arr, v, sizeof(arr))
#define COPY(dst, src) memcpy(dst, src, sizeof(src))